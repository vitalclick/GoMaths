# Build outputs and caches
**/node_modules
**/dist
**/build
**/.next
**/.expo
**/.turbo
**/coverage

# Lockfiles and generated artefacts
pnpm-lock.yaml
package-lock.json
yarn.lock
packages/api-client/src/generated.ts
**/.expo/types/**
# Rewritten verbatim by the Expo CLI ("should not be edited"), so any
# formatting we apply here gets clobbered on the next expo run.
**/expo-env.d.ts

# Designer mockups (visual reference only — see UI/README.md)
UI/

# Curriculum content authored as plain JSON — preserve hand-written shape
curriculum-data/grade-*/

# Playwright reports
e2e/playwright-report/
e2e/test-results/
